## Git
    $ git diff
    $ git status
    $ git add .
    $ git commit
    $ git commit --amend
    $ git rebase -i HEAD~4
    $ git push -u origin master

## Set Up Git Access Token
    $ git config credential.helper store
